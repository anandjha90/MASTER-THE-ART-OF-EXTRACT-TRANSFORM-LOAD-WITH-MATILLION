USE ROLE ACCOUNTADMIN;
USE WAREHOUSE COMPUTE_WH;
USE DATABASE DEMODATABASE;
USE SCHEMA DEMOSCHEMA;
USE SCHEMA PUBLIC;

USE DATABASE DEV_DB;
USE DATABASE test_db;
USE DATABASE prod_db;



CREATE DATABASE DEV_DB;
CREATE DATABASE TEST_DB;
CREATE DATABASE PROD_DB;

CREATE OR REPLACE SCHEMA DEV_SCHEMA;
CREATE OR REPLACE SCHEMA TEST_SCHEMA;
CREATE OR REPLACE SCHEMA PROD_SCHEMA;

CREATE OR REPLACE TABLE CUST_JSON (
DATA_VALUE VARIANT
);

SELECT * FROM CUST_JSON;

CREATE OR REPLACE TABLE IP_RANGE (
DATA_VALUE VARIANT
);

SELECT * FROM IP_RANGE;

DROP TABLE
ip_range;

SELECT * FROM DEMODATABASE.PUBLIC.CUST_JSON;

CREATE OR REPLACE TABLE GW_CUSTOMER_ACCOUNTS (
DATA_VALUE VARIANT
);


CREATE OR REPLACE TABLE GW_ORDERS (
CUSTOMER_RID,
LAST_UPDATED,
ORDER_DATE,
RID
);



CREATE OR REPLACE TABLE GW_ORDER_ITEMS (
ITEM_RID,
LAST_UPDATED,
ORDER_RID,
QUANTITY,
RID
);


CREATE OR REPLACE TABLE GW_ITEMS (
COST,
LAST_UPDATED,
NAME,
PRICE,
RID
);



